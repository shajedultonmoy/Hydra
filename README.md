
34DE-E9E3

…or create a new repository on the command line

echo "# fogotst01" >> README.md
git init
git add README.md
git commit -m "first commit"
git branch -M main
git remote add origin https://github.com/shajedultonmoy/fogotst01.git
git push -u origin main


…or push an existing repository from the command line

git remote add origin https://github.com/shajedultonmoy/fogotst01.git
git branch -M main
git push -u origin main



Git Hub  Commands: (GCL)

gh auth status { checking gh loging or not}

mkdir "foldername"
cd "foldername"
vi "README.md" {t - type, press esc and :wq}
git init
git add README.md {or your file name}
git commit -m "first commit" {replace with commit name}
git branch -M main
git remote add origin https://github.com/shajedultonmoy/fogotst01.git
git push -u origin main

# Hydra
Dev Practice Hydra Repo
